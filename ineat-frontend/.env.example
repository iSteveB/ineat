# InEat frontend environment example
# Copy this file to .env for local development.

# Backend origin without the /api suffix.
# Services build API URLs as ${VITE_API_URL}/api.
VITE_API_URL=http://localhost:3000
